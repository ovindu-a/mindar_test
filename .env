# This environment file is used for development
# to run the app locally using https

HTTPS=true # Required to run the app locally, if not set to true, the app will not run since camera access is only allowed on https

PORT=3000 # Port to run the app on

HOST=0.0.0.0